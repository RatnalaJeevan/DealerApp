package com.wisedrive.dealerapp1.pojos.pojos;

import androidx.core.content.ContextCompat;

import com.github.mikephil.charting.charts.BarChart;
import com.github.mikephil.charting.data.BarData;
import com.github.mikephil.charting.data.BarDataSet;
import com.github.mikephil.charting.data.BarEntry;
import com.wisedrive.dealerapp1.R;

import java.util.ArrayList;

public class abc {


 /*   BarChart barChart = rootView.findViewById(R.id.bar_chart);

// Set chart padding to 0dp
        barChart.setPadding(0, 0, 0, 0);

// Customize chart settings
        barChart.setDragEnabled(true);
        barChart.setScaleEnabled(true);
        barChart.getDescription().setEnabled(false);
        barChart.setDrawGridBackground(false);
        barChart.setDrawBorders(false);
        barChart.getLegend().setEnabled(false);

// Hide axis lines, grid lines, and labels
        barChart.getXAxis().setDrawAxisLine(false);
        barChart.getXAxis().setDrawGridLines(false);
        barChart.getXAxis().setEnabled(false);
        barChart.getAxisLeft().setEnabled(false);
        barChart.getAxisRight().setEnabled(false);

    // Create data set and add data
    ArrayList<BarEntry> entries = new ArrayList<>();
        entries.add(new BarEntry(0, 4));
        entries.add(new BarEntry(1, 5));
        entries.add(new BarEntry(2, 6));
        entries.add(new BarEntry(3, 7));
        entries.add(new BarEntry(4, 8));

    BarDataSet dataSet = new BarDataSet(entries, "Data Set 1");
    int barColor = ContextCompat.getColor(getContext(), R.color.dark_green);
        dataSet.setColor(barColor);
        dataSet.setDrawValues(true);

    // Add data set to chart and refresh
    BarData barData = new BarData(dataSet);
        barChart.setData(barData);
        barChart.invalidate(); */


    // leadCount=(obj.has("leadCount")? obj.getJSONObject("leadCount") : new JSONObject());

               /* if (obj.has("LeadCount")) {
                    JSONObject leadCountObj = obj.getJSONObject("LeadCount");
                    LeadCount leadCount = new LeadCount();
                    leadCount.setCount(leadCountObj.getInt("count"));
                    setLeadCount(leadCount);
                }*/
    //  private  JSONObject leadCount;

}
